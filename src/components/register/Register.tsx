import React from "react";
import { Box, Typography } from "@mui/material";
import { useStyles } from "./useStyles";

const Register = () => {
  const classes = useStyles();

  return (
    <Box>
      <Typography>This is Register screen</Typography>
    </Box>
  );
};

export default Register;
